// 1) ჩაწერე ფაილში ტექსტი, შემდეგ წაიკითხე ეს მონაცემები და დათვალე რამდენი სიტყვაა
// let fs = require("fs/promises")

// async function main() {
    // await fs.writeFile("text.txt", "hello")
    // let readFile = await fs.readFile("text.txt", "utf-8")
    // let lengthFile = readFile.length
    // console.log(lengthFile)
// }

// main()

// 2) ერთი ფაილიდან წაიკითხე ტექსტი, გადაატრიალე (reverse) და ჩაწერე სხვა ფაილში
// async function main() {
//     await fs.writeFile("text2.txt", "group 71")
//     let readFile = await fs.readFile("text2.txt", "utf-8")
//     let reversedFile = readFile.split("").reverse().join("")
//     console.log(reversedFile)
// }

// main()